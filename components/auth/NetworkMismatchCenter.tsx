import { Center } from "@chakra-ui/react";
import NetworkMismatch from "./NetworkMismatch";

const NetworkMismatchCenter = () => {
  return (
    <Center h="calc(100vh)" w="full">
      <NetworkMismatch />
    </Center>
  );
};

export default NetworkMismatchCenter;
